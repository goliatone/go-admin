<section class="site-home-newsletter ui-section">
  <div class="ui-container">
    <h2>{{ home_newsletter.title|default:"Stay current with platform releases" }}</h2>
    <p>{{ home_newsletter.copy|default:"Get practical notes on publishing workflows, localization, governance, and admin extensibility." }}</p>
  </div>
</section>
