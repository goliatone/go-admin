<section class="site-home-news ui-section" data-site-home-news="true">
  <div class="ui-container">
    <h2>{{ home_news.title|default:"Recent product updates" }}</h2>
    <p>{{ home_news.copy|default:"Release notes, workflow improvements, and operational guidance for teams running content-heavy applications." }}</p>
  </div>
</section>
