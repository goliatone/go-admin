<section class="site-home-newsletter ui-section">
  <div class="ui-container">
    <h2>{{ home_newsletter.title|default:"Join the archive newsletter" }}</h2>
    <p>{{ home_newsletter.copy|default:"Get release notes for new teaching collections, translations, and practice guides." }}</p>
  </div>
</section>
