<section class="site-home-news ui-section" data-site-home-news="true">
  <div class="ui-container">
    <h2>{{ home_news.title|default:"Recent archive notes" }}</h2>
    <p>{{ home_news.copy|default:"Fresh editorial context, new transcript links, and updated collections." }}</p>
  </div>
</section>
