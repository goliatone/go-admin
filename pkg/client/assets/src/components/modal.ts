/** Supported public modal entry. Behavioral implementation stays in shared/modal. */
export { ConfirmModal, Modal, TextPromptModal } from '../shared/modal.js';
export type {
  ConfirmModalOptions,
  ModalOptions,
  ModalSize,
  TextPromptModalConfig,
} from '../shared/modal.js';
