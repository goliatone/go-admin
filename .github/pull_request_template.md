## Summary

## Verification

- [ ] I listed the tests or commands I ran.

## Routing Review

- [ ] If this PR changes routes or module contracts, I reviewed the routing diff and summarized it in the PR body.
- [ ] If this PR changes routes or module contracts, I checked `adm.RoutingReport()` and `adm.RoutingPlanner().Manifest()` semantics or updated the owning routing assertions/fixtures.
- [ ] If this PR exposes or changes public API routes, I called that out explicitly.

See `GUIDE_ROUTING.md` for the routing contract and manifest-review workflow.
